{
    "name": "API Gateway in C++",
    "dockerComposeFile":"../docker-compose.yml",
    "service": "api-gateway",
    "workspaceFolder": "/usrapp",
    "postCreateCommand": "sudo chown -R vscode:vscode /usrapp",
    "remoteUser": "vscode",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cmake-tools"
            ]
        }
    }
}